# 2026-08-31

- 从 Pantograph-OCS-analysis（新拉代码）迁移「线路研判」三页到本项目「线路综合分析」分组（替换原 综合分析/线路概况 两页）：
  - 复制组件：MatchingWorkbenchPage（线路简报）、PoleBaselinePage（杆号评估，新组件）、AlarmStatsPage（超限统计，新版）。
  - 复制依赖：composables/useAnalysisQuery.js；utils/loadGuard.js、batchPrefs.js、alarmRules.js、analysisPrefs.js、dateRange.js、api/cache.js（新版含 inflight/cachedGet）；FilterBar.vue（新版支持批次选择/多组对比/range 模式，向后兼容 defaultRangeMonths）。
  - App.vue 合并接线（保留本项目自定义样式）：manualBatch/batchByDate/batchesByDate/queryNonce 状态、provide('analysisQueryNonce')、filterEnableBatchPicker/enableMultiBatchCompare 计算属性、批次变更处理与持久化（batchPrefs）、adjustDatesForPage 支持 defaultSelectCount、loadDates 加 seq 防竞态 + filterSwitching 防回灌。
  - api/client.js：新增 fetchDayBatches（/day-batches，cachedGet）、postPoleBaseline（/pole-baseline）、postCorrelationDiagnose 改走缓存。
  - NavMark.vue：新增 pole 杆位图标；modules.js：line-risk 分组替换为三页。
  - 对三个新页面重做 width→min-width 表格自适应和硬编码颜色主题化（与全站规范一致）。
  - vite build 验证通过。注意：后端需提供 /pole-baseline、/day-batches 端点，/correlation/diagnose 需支持检测组 payload 字段。
- 更新「速度-燃弧图」页（SpeedArcPage.vue）为 Pantograph-OCS-analysis 新版：
  - 新增散点「燃弧强度 ⇄ 燃弧时间」切换开关（showArcTime，右轴 ms/A²·s 联动）；散点只保留正值、tooltip 同时展示强度+时间。
  - 改用 useAnalysisQuery + analysisFingerprint/analysisPayload 架构（缓存 /speed-arc、批处理、防竞态、keep-alive 感知），去掉 fetchSpeedArcMeta 旧调用链；速度无效值置 null 断开连线（connectNulls:false + sampling lttb）。
  - 新增区间空态提示（残缺/折返小批）、响应式 el-col、图表 key 强制 notMerge 重渲染。
  - chartOptions.js 新增 legendSelectedKeepFirst（>8 条曲线只亮前 4 条）；modules.js 的 speed-arc 配置加 defaultSelectCount:2 + enableBatchPicker/enableMultiBatchCompare。
  - 依赖均已就绪（App.vue pageProps 已传批次 props，useAnalysisQuery 已导出 cacheKey/getCached），vite build（dist-verify4）通过。
