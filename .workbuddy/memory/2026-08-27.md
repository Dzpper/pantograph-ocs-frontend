# 2026-08-27

- 给左侧树形菜单的二级菜单（分组节点）加上图标：
  - `src/config/modules.js`：为每个分组新增 `icon` 字段（strip-data/climate-data → layers，line-risk → target，data-analysis → list，focus-report-group → report，sys-lines → shield，sys-log → log）。
  - `src/components/common/NavMark.vue`：新增 layers / target / shield 三个 SVG 图标。
  - `src/App.vue`：分组 el-sub-menu 标题插入 `<NavMark :name="group.icon || 'dot'" />`，并加 `.om-tree-menu :deep(.nav-mark) { margin-right: 8px }` 间距。
  - `vite build` 验证通过。
